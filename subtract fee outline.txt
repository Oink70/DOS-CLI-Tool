Steps needed to subtract the fee from a balance in order to send complete balance through DOS batch file.

1)	Problem: Get Balance
	Solution: %FILELOCATION%/verus z_getbalance "%Z-ADDRESS%"

2) 	Problem: Get output of 1) in a variable
	Solution: For /F "delims=" %%I in ('%FILELOCATION%/verus z_getbalance "%Z-ADDRESS%"') Do Set BALANCE=%%I

3)	Problem: Determine if Balance is greater than fee
	Possible Solution: if FEE>BALANCE do









4)	Problem: Determine length of BALANCE string variable
	Solution: 
		call :strLen BALANCE strlen

		:strLen
		setlocal enabledelayedexpansion
		:strLen_Loop
		   if not "!%1:~%len%!"=="" set /A len+=1 & goto :strLen_Loop
		(endlocal & set %2=%len%)
		goto :eof

